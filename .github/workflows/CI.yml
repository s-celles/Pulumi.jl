name: CI
on:
  push:
    branches:
      - main
    tags: ['*']
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  test:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        version:
          - '1.10'
          - '1'
        os:
          - ubuntu-latest
        arch:
          - x64
    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
          arch: ${{ matrix.arch }}
      - uses: julia-actions/cache@v2
      - name: Clone unregistered dependencies
        run: |
          git clone https://github.com/s-celles/gRPCServer.jl /tmp/gRPCServer.jl
      - name: Install and build
        run: |
          julia --project=. -e '
            using Pkg
            Pkg.develop(PackageSpec(path="/tmp/gRPCServer.jl"))
            Pkg.instantiate()
            Pkg.build()'
      - name: Run tests
        run: |
          julia --project=. -e '
            using Pkg
            Pkg.test()'
